# Integration Tests

Testing how components work together:
- Backend GraphQL + MongoDB
- Frontend Apollo Client + Backend
- Stripe payment flow
- Real-time messaging (WebSockets)

## Test Scenarios
- [ ] Full authentication flow (UI → GraphQL → MongoDB)
- [ ] Property listing flow with search filters
- [ ] Payment flow (UI → Stripe → MongoDB)
- [ ] Messaging flow (UI → GraphQL → MongoDB → WebSocket)
